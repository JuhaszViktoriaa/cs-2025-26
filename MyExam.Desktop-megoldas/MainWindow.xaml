<Window x:Class="MyExam.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyExam.Desktop"
        xmlns:viewmodel="clr-namespace:MyExam.Desktop.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <viewmodel:StatisticsViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding CountText}" Margin="5"/>
        <TextBlock Grid.Row="1" Text="{Binding RevenueText}" Margin="5"/>
        <TextBlock Grid.Row="2" Text="{Binding RevenuelessText}" Margin="5"/>
        <TextBlock Grid.Row="3" Text="{Binding NagyannaText}" Margin="5"/>
        <TextBlock Grid.Row="4" Text="{Binding BetweenrevText}" Margin="5"/>
    </Grid>
</Window>